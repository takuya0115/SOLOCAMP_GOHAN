<%= form_with(model: [recipe, comment], url: recipe_comments_path(@recipe), local: false) do |f| %>
    <%= f.text_area :comment, class: "input-mysize" %></br>
    <%= f.submit "送信", class: "btn btn-success comment-submit float-right" %>
<% end %>
